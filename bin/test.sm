Statemachine Door {
  Start State Closed {
    ON push IF (2 * 2 == 4 OR 6 / 2 == 3)  GO Open
    ON kick IF (64 / 2) != 32 GO Closed
    ON lock GO Locked
  }

  State Open {
	  ON pull GO Closed
    ON slam IF (2 * 2) > 4 GO Closed
  }

  Final State Locked {
	  ON unlock GO Closed
  }
}